services:
  - type: web
    name: pnl-automatonsx
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATA_DIR
        value: /data
    disk:
      name: pnl-data
      mountPath: /data
      sizeGB: 1
